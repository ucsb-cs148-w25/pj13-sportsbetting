@startuml
start
:User opens app;
if (Signed In?) then (Yes)
    :Redirect to home screen;
else (No)
    :Show login/sign-up page;
endif
:User navigates to betting page;
:User selects a bet;
:User places bet;
:Bet stored in Firebase;
:User checks leaderboard;
:User checks injury updates;
:Daily cron job updates bets;
stop
@enduml
